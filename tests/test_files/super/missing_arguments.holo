class Base {
  foo(a, b) {
    print "Base.foo(" + a + ", " + b + ")";
  }
}

class Derived : Base {
  foo() {
    super.foo(1); // expect runtime error: Incorrect number of arguments: expected 2, got 1
  }
}

Derived().foo();
